import { environment } from '../../../environments/environment';

export const LoginEndpoint = environment.apiUrl + 'account/login';

export const RegisterEndpoint = environment.apiUrl + 'account/register';
