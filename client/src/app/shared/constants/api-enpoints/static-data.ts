import { environment } from '../../../../environments/environment';

export const GetCountriesEndpoint = environment.apiUrl + 'Countries';

export const GetCitiesEndpoint = environment.apiUrl + 'Cities';
